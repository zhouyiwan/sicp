10 ; 10

(display (+ 5 3 4)) ; 12

(- 9 1) ; 8

(/ 6 2) ; 3

(+ (* 2 4) (- 4 6)) ; 6

(define a 3) ; 为名字a绑定值3

(define b (+ a 1)) ; 为名字b绑定值4

(+ a b (* a b)); 3 + 4 + (3 * 4) = 19

(= a b); #f 为假

(if (and (> b a) (< b (* a b)))
    b
    a); 4 如果 b > a 并且 b < a * b 转化为 如果 4 > 3 并且 4 < 12 = #t 所以返回4

(cond ((= a 4) 6)
      ((= b 4) (+ 6 7 a))
      ((else 25))); 16

(+ 2 (if (> b a) b a)); 6

(* (cond ((> a b) a)
         ((< a b) b)
         (else -1))
   (+ a 1)); 16